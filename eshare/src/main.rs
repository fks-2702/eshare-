extern crate ft;
use std::path::PathBuf;
use std::env;
use std::fs::File;

fn main() {
    
    
    let flag = std::env::args().nth(1).expect("no flag given");
    let file = std::env::args().nth(2).expect("no file given");    
    println!("flag: {:?}, path: {:?}", pattern, path)
    


}
